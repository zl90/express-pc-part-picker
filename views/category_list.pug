extends layout 

block content
    div(class='list-title-container')
        h1= title
        a(href='/category/create') + Add New Category

    ul(class='list-container')
        each val in category_names
            li
                a(href=val.url class="list-item-link")
                    button(class='list-item-button') #{val.name}
